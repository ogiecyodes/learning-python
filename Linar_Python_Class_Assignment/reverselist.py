def reverselist(numbers):
  numbers.reverse()
  return numbers
  #x = numbers[::-1]# :: prints everyting in the list start from the end -1
  #return x
value = reverselist(numbers=([1,2,3,4,5]))
print(value)